# Emboss Lab 0.3.0: 文字图层与真实材质

- 文字图层支持独立字体、字号、字距、位置与凹刻设置，可同时排版凸字和凹字。
- 画布文字支持拖拽吸附、滚轮缩放；点击画板外可隐藏选中框。
- 内置思源黑体、思源宋体的简体中文和日文版本，字体随应用分发。
- 多种材质接入本地纹理，补充粗糙度、表面细节、风化与光泽表现。
- 增强高光 Boost、Bloom 和金属闪光效果。
- 优化交互预览，收细滑杆，支持双击滑杆恢复默认值。
- 修复大字提前裁切、整体垂直移动限位、小字光影过重和图层控件同步问题。
- 默认启动排版为「刻于无形 / CARVE THE INVISIBLE」，包含独立凹凸和完整材质、灯光设置。
- 混合凹凸高度图以中灰为平面，亮色为凸起、暗色为凹陷。

Windows: portable x64。Mac 可运行 `npm ci` 后使用 `npm run build:mac:arm64` 或 `npm run build:mac:x64`；签名及公证沿用项目原有配置。

---

# Emboss Lab 0.2.0: Pattern & Type 更新

这次更新的变化很直接：Emboss Lab 不再只能从图片开始。现在可以在应用里直接输入文字，做字标、姓名缩写和连续纹样，不需要先去其他软件生成 PNG。

## Type / Monogram

- 直接输入文字，支持字重、字距和斜体。
- 新增 10 款开源字体，包括 Outfit、Montserrat、Roboto、Inter、EB Garamond、Playfair Display、Cormorant Garamond、Instrument Serif、Great Vibes 和 Caveat。
- 字体按无衬线、衬线、花体和手写分组，也可以导入 TTF、OTF、WOFF 或 WOFF2 字体。
- 默认使用 EB Garamond 和 1:1 画布，启动内容改为 `EMBOSS LAB`。
- 重新计算字形边界，手写体、斜体和下伸笔画不再被裁掉。
- 文字移动和放大时会保持在画布内。

## Pattern

- 可以把一个图形或字标排成连续纹样。
- 提供网格、砖式错排、半落式和镜像四种重复方式。
- 可以调整图案大小、间距、旋转和错位比例。
- 新增无缝边缘和 Pattern Tile 导出，方便继续用在包装、布料和品牌视觉中。

## 材质与反射

- 金属材质新增“反射尺寸”和“反射变化”。
- 金箔默认改成更宽、更少重复的反射带。
- 表面纹理和金属反射现在是两套独立参数，不会再混在一起。

## 画质与速度

- 调整参数时先显示轻量预览，停止操作后再自动精算。
- Type 高度源提升到 2400px，放大文字时更干净。
- 浮雕高度改为平滑采样，减少锯齿和小图重复后的劣化。
- 修复 Pattern 模式下 Composition 面板的排版问题，也修正了重复排列时浮雕高度不一致的情况。

现在的基本流程是：输入图片或文字，选择单图、Pattern 或 Mosaic，再调材质和灯光，最后导出成片。

## 社媒短版

Emboss Lab 这次加了 Pattern 和 Type / Monogram。现在可以直接输入文字、选开源字体或导入自己的字体，再把字标排成连续纹样。

这轮也重做了文字清晰度和重复排列的采样，修了字形越界、小图高度变化和 Pattern 面板排版。金箔、铝板和铬面也多了独立的反射尺寸与变化控件。
