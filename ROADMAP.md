# Emboss Lab 下一版本计划

下一版可以把边框作为主功能。Type 和 Pattern 解决了“放什么进去”，边框则能补上构图的最后一步，对字标、邀请函、证书、包装和婚礼类内容都有用。

## 主功能：Frames / Borders

第一版不需要做得太大，先放 12 到 16 个真正常用的边框：

- 细线、双线、圆角和内外双框。
- 邮票齿、票券切角和标签轮廓。
- Art Deco 几何边框。
- 少量古典装饰边框，不要一开始就做成庞大的素材库。

配套控件建议包括：

- 边框粗细、内边距和圆角。
- 整体缩放和横竖比例。
- 边框浮雕高度，与中央图形分开调整。
- 边框可以跟随当前材质，也可以只作为透明 Overlay。
- 支持导入用户自己的 PNG 或 SVG 边框。

内置边框最好用参数化路径生成，不要只放固定尺寸的 PNG。这样放大后不会糊，1:1、4:5 和 16:9 也能共用同一套素材。

## 需要同时继续的工作

### 性能第二轮

- 把高度图和浮雕计算移到 Worker 或 OffscreenCanvas，减少主界面卡顿。
- 继续检查 Pattern 和大尺寸导出的内存占用。
- 为材质、字体和重复纹样建立更细的缓存。

### Type 补完

- 增加行距、对齐和更好的多行排版。
- 将内置开源字体改为本地资源，离线也能使用。
- 检查不同字重和手写体的实际可用范围，避免浏览器合成不存在的字重。

### 预设与导出

- 允许保存自己的材质和灯光预设。
- 为导出增加最后一层边界检查，保证预览、PNG、高度图和 Pattern Tile 的位置一致。
- 补一轮大图导出的进度与失败提示。

### 桌面版发布

- 固定一套 macOS 签名、公证和安装验证流程。
- 每次发布前在 Apple Silicon 和 Intel Mac 上各做一次干净安装。
- 对下载损坏、Gatekeeper 拦截和授权失败分别给出明确提示，不要再让用户只看到“文件已损坏”。

## 建议的优先级

1. 先完成边框的参数系统和 4 个基础样式。
2. 扩展到 12 到 16 个内置边框，同时补导入、高度和导出。
3. 完成性能和离线字体。
4. 最后做桌面包的签名、公证和真机安装测试。

